# Task Scripts
---
platform: linux

image_resource:
  type: docker-image
  source: {repository: ubuntu}

inputs:
  - name: examples # mount pwd to execute the sh script

run:
  path: /bin/sh
  args: ["examples/task-script.sh"]
